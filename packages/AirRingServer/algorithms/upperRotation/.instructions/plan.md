# 计划（Plan）

## 核心目标（持续任务）

优化上旋估算，使行为符合预期。

当前阶段目标：

1. 保持模拟器数据集通过
2. 使真实数据集（DS01-DS05）通过（`误差 < 5 deg`）
3. 若无法全部通过，提供证据支撑的根因分析和缓解计划

## 当前状态目标

- 模拟器套件：应保持全部通过
- 真实套件：当前失败，需要重点优化

## 执行清单

- [ ] 使用可复现命令和日志确认最新基线
- [ ] 按真实数据集隔离失败模式（DS01..DS05）
- [ ] 评估 `auto` 仲裁（`evaluateExpanded` vs `evaluateDirect`）对真实数据集的影响
- [ ] 调优边界/分歧回退阈值，避免错误的模式切换
- [ ] 重新评估加速度比策略（固定 vs 自适应）
- [ ] 验证算法改动后无模拟器回归
- [ ] 必要时仅在确认不匹配来自模拟器侧时调整模拟器行为
- [ ] 每次迭代更新 `progress.md`；遇到非平凡决策时更新 `decisions.md`

## 立即采取的下一步行动

1. 运行真实和模拟器套件，存储简洁证据快照
2. 实施最小的安全算法改动以解决真实数据错误
3. 重新运行套件并比较增量；仅在净改进且无模拟器回归时保留改动

## 本阶段的完成定义

优先选项：

- DS01..DS05 全部通过（`< 5 deg`）且模拟器套件保持通过

后备交付物（无法达成优先选项时）：

- 按数据集分类的失败分类法
- 按优先级排序的根因候选与支撑日志
- 具体的下一步补丁计划、预期影响和风险
