# 动态局部搜索窗口计划

1. 定义特征候选、显式不确定度分量、全局角度边界和搜索预算输入。
2. 以不确定度分量绝对值之和构造保守半径，并应用调用方显式最小半径。
3. 将窗口裁剪到全局合法角度范围，输出左右裁剪诊断和实际跨度。
4. 根据显式搜索步长计算搜索点预算；窗口退化或超过预算时拒绝。
5. 不包含 DS 角度带、固定局部半径或生产默认阈值，不接入现有搜索路径。
6. 添加随机性质、边界裁剪、预算和非法输入测试；运行格式化、测试与类型检查。
7. 更新任务计划、进度、决策与长期上下文。
