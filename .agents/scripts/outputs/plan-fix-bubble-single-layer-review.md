# Plan: 修复纵向（单层）审查问题

1. 修复实时扫描趟在 `endTs` / `pointCount` 增长时的样本与重建缓存失效，并触发重新计算。
2. 保留滑动窗口用于求解 `B(phi)`，但展示层仅使用当前 baseline 时间范围内的样本分解。
3. 在重建结果中返回工艺变形因子，tooltip 使用 `eta * (b1 + b2)` 表示压合双层预测厚度。
4. 将自动缩放与显示平滑改为显式选项，默认关闭；补充 raw profile 与 raw residual 诊断字段，并统一类型注释默认值。
5. 运行 lint 与相关 TypeScript typecheck（不运行单元测试），记录既有错误与本次结果。

## 验收

- live 模式下同一扫描 ID 的 `endTs` 或 `pointCount` 变化会重新加载并重建。
- 图表上下层数据均来自当前选中扫描趟时间范围。
- tooltip 的“压合双层预测厚度”与算法正模型一致。
- 默认返回未经隐式缩放/额外卷积平滑的求解结果；调用方可显式开启后处理。
