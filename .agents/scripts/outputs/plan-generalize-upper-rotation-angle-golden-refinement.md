# 有界黄金分割细化计划

1. 从已接受离散扫描的最佳点向两侧寻找最近的有限 loss 支撑点，形成局部括区间。
2. 括区间缺少任一侧证据时拒绝，不跨越动态窗口补点。
3. 使用调用方显式角度容差、迭代上限和边界保护距离执行黄金分割。
4. 细化过程中目标异常或非有限 loss 时拒绝，不用插值值替代真实目标函数。
5. 未在迭代预算内收敛或结果贴局部/全局窗口边界时拒绝。
6. 输出离散与细化 loss 差、最终括区间、迭代数和边界距离诊断。
7. 添加随机凸目标、缺失支撑、异常目标、预算与边界测试，运行格式化、测试和类型检查。
8. 更新任务计划、进度、决策与长期上下文。
