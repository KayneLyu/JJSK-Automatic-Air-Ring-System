# 离线通用局部精调管线计划

1. 组合动态窗口、离散扫描和黄金分割为单一纯函数入口。
2. 每一层失败时停止后续计算，保留已产生的完整阶段诊断和失败阶段。
3. 输出特征角度、细化角度、两者偏移、最终 loss、不确定度及证据来源。
4. 使用调用方显式最大允许特征/目标偏移检测双证据冲突。
5. 冲突时拒绝，不选择特征值或目标函数值作为兜底。
6. 添加成功、三阶段拒绝和证据冲突测试；运行格式化、相关测试和类型检查。
7. 更新任务计划、进度、决策与长期上下文。
