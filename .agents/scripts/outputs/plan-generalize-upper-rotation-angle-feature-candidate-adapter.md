# Plan: Stage 6 特征粗估输入适配器

1. 读取多行程聚合与逐行程置信证据结构，定义 Stage 7 所需的只读粗估输入。
2. 将角度 MAD、正反向中位数差、已观测趋势跨度和显式端部时间不确定度保留为具名分量，不合成经验权重分数。
3. 校验聚合接受状态、候选数量一致性及每个已接受行程的置信证据完整性，分层输出拒绝原因。
4. 添加成功、顺序不变、聚合拒绝、证据缺失、数量不一致和非法不确定度测试。
5. 运行格式化、针对性测试、差异检查和 AirRingServer typecheck，并更新任务记录。
