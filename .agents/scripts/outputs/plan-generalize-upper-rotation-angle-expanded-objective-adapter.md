# Plan: evaluateExpanded 只读目标适配器

1. 定义 expanded 行程输入、显式最低证据要求和结构化诊断。
2. 在不修改输入的前提下校验行程与点，保留 `NaN` 厚度的缺测语义，拒绝无效物理参数。
3. 暴露可直接供 Stage 7 局部精调管线使用的 `angle → loss` 回调，不接入默认生产路径。
4. 添加适配等价性、只读性、无效输入、证据不足和管线兼容性测试。
5. 运行格式化、相关单测、差异检查和 AirRingServer typecheck，并更新任务记录。
