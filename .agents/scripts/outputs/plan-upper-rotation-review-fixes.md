# 上旋最大角度 review 问题修复计划

1. 为厚度信号和 loss 曲线增加可辨识性检查，无信息数据返回 `null`。
2. 将梯形运动模型的加减速比例限制在物理有效区间，并防御非有限输入。
3. 让 `deltaRange.step` 实际控制搜索步长，同时保留最终细化精度。
4. 改进不完整行程过滤，避免短首尾片段主导中位数且不再回退污染数据。
5. 添加聚焦的回归测试；按仓库约定不执行测试，仅运行类型检查与 lint。

