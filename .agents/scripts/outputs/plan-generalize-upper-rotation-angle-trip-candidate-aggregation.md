# 多行程角度候选聚合计划

1. 定义独立的行程角度候选与聚合诊断结构，不接入生产路径。
2. 过滤被上游拒绝或数值无效的候选，并由调用方指定最少有效行程数。
3. 强制至少包含一个正向和一个反向行程，使用中位数聚合角度。
4. 输出 MAD、样本标准差、正反方向中位数差及按行程序列的线性趋势。
5. 使用调用方显式趋势上限拒绝漂移，不内置历史样本阈值。
6. 添加性质与拒绝路径测试，执行格式化、相关测试和类型检查。
7. 更新任务进度、计划、决策与长期上下文。
