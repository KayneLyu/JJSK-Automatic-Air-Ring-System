# 通用目标函数局部扫描计划

1. 让动态窗口结果携带经预算验证的搜索步长。
2. 定义只依赖 `angle → loss` 回调的局部离散扫描纯函数，不耦合历史策略。
3. 覆盖窗口两端并限制实际评估数量不超过窗口计划预算。
4. 非有限 loss 或目标函数异常计为无效评估；全部无效时结构化拒绝。
5. 最佳有限 loss 位于任一窗口边界时拒绝，保留完整扫描诊断。
6. 添加凸目标恢复、边界最优、无效 loss 与无效窗口测试。
7. 运行格式化、相关测试和类型检查，并更新任务文档。
