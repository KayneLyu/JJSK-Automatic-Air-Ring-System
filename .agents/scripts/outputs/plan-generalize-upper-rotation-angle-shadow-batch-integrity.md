# Plan: 影子批次完整性与工况覆盖

1. 将批量统计配置改为显式对象，包含最低记录、机台、配方数量及元数据完整性要求。
2. 在统计前校验记录结构、唯一 window ID 和严格递增观测时间，异常批次整体拒绝。
3. 输出观测时间跨度、不同机台/配方数量、缺失元数据数量及各工况记录计数。
4. 按调用方显式覆盖要求拒绝机台/配方不足或元数据不完整的批次，不设置生产默认值。
5. 扩展测试覆盖完整批次、重复 ID、乱序/同刻时间、缺失元数据、覆盖不足和非法记录。
