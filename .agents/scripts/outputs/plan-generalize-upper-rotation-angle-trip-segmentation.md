# 换向去抖与完整行程验证计划

1. 定义只读方向采样与完整行程边界类型，不修改设备连接和现有生产分段器。
2. 将互斥的正反转布尔信号规范化为方向；单次双真/双假信号计为无效，不触发换向。
3. 使用调用方显式提供的连续稳定样本数确认方向状态与换向起点。
4. 仅输出两个已确认换向边界之间的完整行程，排除采集开头和结尾的不完整片段。
5. 用调用方显式行程时长范围拒绝过短或过长片段，并输出诊断计数。
6. 添加去抖、不完整边界、无效信号、时序和配置测试；运行格式化、测试和类型检查。
7. 更新任务计划、进度、决策与长期上下文。
