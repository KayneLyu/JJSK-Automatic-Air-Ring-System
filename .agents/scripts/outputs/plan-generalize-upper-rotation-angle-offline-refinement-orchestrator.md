# Plan: 通用角度离线精调编排入口

1. 定义组合 Stage 6 候选、expanded 数据和显式精调配置的只读输入类型。
2. 按“特征粗估适配 → 目标函数适配 → 局部精调”顺序短路执行，保留每层完整诊断。
3. 输出统一的接受状态、最终角度、loss、拒绝阶段和拒绝原因，不提供生产默认配置。
4. 添加成功等价、特征适配拒绝、目标适配拒绝和精调拒绝测试，并确认不修改输入。
5. 运行格式化、相关测试、差异检查和 AirRingServer typecheck，并更新任务记录。
