# Plan: generalize-upper-rotation-angle 速度稳定性判定

1. 在现有角速度聚合结果之上增加独立稳定性判定层，保持生产入口不变。
2. 要求调用方显式提供相对 MAD 与最大绝对偏差上限，并严格校验边界。
3. 输出逐项测量值、限值、越界状态和结构化拒绝原因，不合成经验权重分数。
4. 增加稳定、单项越界、双项越界与非法输入测试，运行角速度最小测试集。
5. 追加更新 task plan、progress 和 decisions；不提交、不推送。

