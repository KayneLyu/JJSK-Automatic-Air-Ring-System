# Plan: 三路径影子记录与批量统计

1. 定义可序列化的生产、generic 和新离线路径影子记录，生产选择保持不变。
2. 从现有策略对照与离线精调结果提取角度、耗时、拒绝阶段/原因和可比较差值，不保留函数或大数组。
3. 实现无标签批量统计：各路径接受率、两两差值分布、耗时分布和新路径拒绝原因计数。
4. 对记录元数据、耗时和最少样本数做结构化校验，不将路径间一致性解释为准确度。
5. 添加生产选择不变、拒绝记录、差值分布、拒绝计数和非法输入测试；运行格式化、测试、typecheck 与差异检查。
