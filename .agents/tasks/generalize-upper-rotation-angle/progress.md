# Progress: 上旋最大角度算法通用化

## 2026-07-27 11:06

- 识别为新的复杂任务，预计跨多个模块并需要多轮现场验证，创建独立 task 目录。
- 已完成静态风险评估：当前 H1/H2/C5/DS05-like 存在历史样本定向优化痕迹；现有互相关实现尚不足以直接替代生产算法。
- 已制定“策略隔离 → 扫描预处理 → 特征追踪 → 角速度 → 端部补偿 → 局部精调 → 影子运行 → 切换清理”的分阶段计划。
- 当前主工作目录在任务索引中属于其他活跃任务；按 One Task One Stream 规则，本任务登记为 planned，等待用户准备并切换独立分支/worktree。
- 本轮没有创建 task 脚本，没有运行测试，没有修改生产代码。

## 2026-07-27 11:14

- 用户明确要求复用当前目录与 `main` 分支，确认覆盖默认的 One Task One Stream 隔离策略；任务索引已更新为 active。
- 实施阶段 1 的首个最小动作：让 `strategyProfile` 真正隔离历史数据集定向规则。
- `generic` 现在跳过 H1、H2、C5 和 DS05-like；标准高角度 challenger 仍保留为通用候选机制。
- 迁移期间默认 profile 改为 `datasetTuned2026Q1`，保持未验收前的当前生产行为不变。
- 本轮未运行测试；仅执行静态差异检查。

## 2026-07-27 11:44

- 按用户要求移除项目中的“不自动执行单元测试”规则，统一改为代码修改后主动运行最小相关测试集、阶段验收运行测试矩阵。
- 新增 `strategyProfile.test.ts`，验证默认路径与显式 `datasetTuned2026Q1` 一致，并验证 `generic` 不触发 DS01 的 H1 与 DS05-like 高角度过估修正。
- 真实数据集测试：1 个文件、5 个用例全部通过；误差分别约 0.25°、0.20°、4.44°、4.40°、2.17°。
- profile 隔离测试：1 个文件、2 个用例全部通过；DS01 generic 返回约 310.4°，DS05 generic 返回约 335.1°，证明定向修正已被隔离。
- 模拟器回归：10 个文件、10 个用例全部失败，误差约 7.76°～153.92°；运行日志确认均使用默认 `datasetTuned2026Q1`，本次变更未改变该路径逻辑，记录为当前仓库既有基线问题，后续阶段 0 需单独核对仿真数据与算法模型。
- `pnpm exec vitest` 首次因 pnpm 10.18.3 registry 签名/获取失败未进入测试；随后使用已安装的本地 `./node_modules/.bin/vitest` 完成验证，未下载依赖或修改锁文件。
- 真实数据测试同时观察到单次估算约 163～500ms，超过项目 100ms 目标，纳入后续性能基线。

## 2026-07-27 11:52

- 完成阶段 0 的数据资产与标签来源审计。
- DS01～DS05 文件完整，但角度标签没有独立测量来源说明，且已经参与定向规则开发，正式降级为开发回归集。
- 模拟器角度具有配置真值，但 10 个测试直接使用不可追踪的 `Math.random()`，失败后无法重放且本轮基线 10/10 失败；需要改为记录 seed 与完整生成参数的分布驱动随机测试。
- 2026-05-22 ModBus 原始日志位于仓库外且当前可读，但现有测试没有独立角度标签，只能作为无标签现场诊断集。
- 2026-06-10 ADBox 测试所需两份原始日志当前缺失，暂不能重放。
- 长期上下文中的 `295.946411°` 和 `306.022472°` 未找到独立实测来源，不能作为准确度真值。
- 当前调参集与冻结验收集均为空；在补充独立机械/PLC/人工角度真值前，不允许切换生产默认或删除旧回退路径。
- 本轮仅更新任务文档，没有修改生产代码，因此未运行单元测试。

## 2026-07-27 14:29

- 用户指出“固定模拟器角度会再次诱发定向优化”，否决将 10 个随机用例替换为固定角度的思路。
- 修正方案为分布驱动的随机/性质测试：持续从物理范围抽取角度和工况，不维护有限固定角度验收集。
- 可复现性通过记录随机 seed 和完整生成参数实现；失败 seed 仅用于诊断重放，不作为长期定向调参目标。
- 阶段验收改为评价误差分布、最坏误差、拒绝率和覆盖范围，不再以少数固定样本是否通过作为泛化证据。
- 本轮只修正文档和决策，没有修改测试或生产代码，因此未运行单元测试。

## 2026-07-27 14:37

- 将模拟器测试从不可追踪的 `Math.random()` 改为 seed 驱动的分布生成器，未引入第三方依赖，也未固定角度样本。
- 每次随机生成 `θmax`、上旋单程时间、扫描周期、测量噪声和流量扰动，并在日志中输出完整场景参数与重放命令。
- 仿真器内部的流量和测厚噪声也绑定到由同一 seed 派生的随机流，避免只复现场景配置却不能复现数据序列。
- 支持用 `UPPER_ROTATION_REPLAY_SEED=<seed>` 重放单个失败场景；无该变量时使用系统随机 seed，持续产生新样本。
- 新增生成器性质测试：相同 seed 场景一致、不同 seed 场景不同、100 个 seed 的所有参数均在物理测试范围内；2/2 通过。
- 随机场景 seed `1799103169` 首次结果为 `θmax=305.3° → 180.01°`、误差 `125.29°`；连续两次重放的参数、点数、loss 和结果完全一致，证明完整重放能力生效。
- 随机场景算法测试仍失败，这是当前通用模型基线问题；未针对该 seed 修改任何算法阈值或规则。

## 2026-07-27 14:52

- 新增 `estimateThetaMaxWithPhaseCorrectionDetailed`，保持原 `estimateThetaMaxWithPhaseCorrection` 的 `number | null` 接口和计算核心不变。
- 详细结果记录状态、profile、目标/offset模式、实际目标函数、输入/完整/过滤行程数、搜索点数、基础角度、最终角度、最终loss、触发规则、拒绝原因和总耗时。
- tuned路径可结构化报告 `lowAngleH1`、`lowAngleH2`、`highAngleC5`、`highAngleOverEstimation` 等规则；generic路径对应定向规则列表为空。
- 为完整行程缺失等不可估计情况提供稳定的机器可读拒绝原因，不再只能解析控制台文本。
- profile回归测试扩展为4个用例并全部通过：旧接口兼容、结构化成功诊断、generic/tuned隔离和结构化拒绝。
- 观察到DS01 tuned两次估算约206～244ms、generic约143ms，诊断耗时字段可用于后续影子性能比较。

## 2026-07-27 15:06

- 新增 `compareUpperRotationStrategies` 显式影子对照API，同一输入依次运行 tuned生产路径和generic影子路径。
- 对照结果包含两侧完整诊断、生产选择角度、有符号/绝对角度差、耗时差和可比较状态。
- 迁移期间 `selectedThetaDeg` 始终取 tuned 结果；generic只提供影子证据，不改变现有生产输出。
- 原 `estimateThetaMaxWithPhaseCorrection` 不会自动执行影子路径，因此线上默认性能不受影响；只有显式调用对照API才承担两次估算开销。
- profile回归测试扩展为5个用例并全部通过；DS01 tuned触发H1、generic规则列表为空，绝对角度差大于20°。
- 阶段1的策略隔离、结构化诊断和显式影子对照均已完成；下一阶段进入扫描剖面预处理。

## 2026-07-27 15:21

- 新增独立 `normalizeScannerProfile` 模块，暂不接入生产估算链路。
- 输入点按物理位置排序并重采样到固定网格，正向和反向扫描得到相同空间方向的结果，不再按采样序号假设方向。
- 非有限位置/厚度点被过滤，同一位置的重复读数先平均；网格覆盖范围外保留 `NaN`，避免把未知边缘伪造成有效厚度。
- 输出输入点数、有效点数、唯一位置数、有效率、覆盖率、缺失率和原始扫描方向；有效点或覆盖不足返回结构化拒绝原因。
- 新增4个单元测试并全部通过：正反方向等价、不规则位置插值与重复点合并、无效/缺失保留、覆盖不足拒绝。
- 使用普通逆序循环替代 `Array.findLast`，保持项目ES2022目标兼容。
- 下一轮继续实现边缘裁剪、去均值/去趋势和可选轻度平滑；在性质测试通过前仍不接入特征追踪。

## 2026-07-27 15:44

- 完成阶段 2 的扫描剖面预处理能力，仍保持为独立离线模块，未接入生产估算链路。
- 新增显式可选的按比例对称边缘裁剪、有限值去均值、最小二乘线性去趋势和缺失值感知的中心移动平均；三项默认均关闭，不改变原始重采样结果。
- 平滑只使用窗口内有限邻点，原本缺失的中心位置继续保持 `NaN`；裁剪后重新计算缺失率。
- 预处理选项仅按数学边界校验，非法裁剪比例、非整数/过宽平滑半径返回 `invalidPreprocessingOptions`，未依据历史角度样本设定参数。
- 扫描标准化单元测试扩展为 9 个并全部通过，覆盖去趋势、裁剪、平滑、缺失保留、正反向等价和非法配置拒绝。
- AirRingServer 全量类型检查未通过；本轮暴露的两个 ES2023 `toReversed` 用法已改为 ES2022 兼容写法，剩余错误来自既有 `fft-js` 声明缺失、旧测试导入语法、失效文件路径和 `confirmCount` 类型不一致。
- 下一轮进入阶段 3，先实现逐位移、按真实重叠区计算的 NaN 感知 ZNCC 核心，不接入生产路径。

## 2026-07-27 16:42

- 新增独立 `calculateZncc` 核心，尚未替换既有互相关函数，也未接入生产上旋估算路径。
- 每个整数位移使用该位移下双方同时有限的真实重叠点，分别重新计算均值、方差和零均值归一化相关系数，避免全剖面归一化造成边缘偏差。
- 搜索窗口 `maxShift` 和最低重叠点数 `minOverlapCount` 必须由调用方显式提供；核心只做相对输入长度的数学边界校验，不包含固定角度窗口或历史样本阈值。
- 长度不一致、非法选项、所有候选零方差或重叠不足时返回结构化拒绝原因，不静默截断或输出伪相关峰。
- 新增 4 个性质测试，包含 64 组随机长度、平移、幅值缩放、偏置和部分缺失场景，以及重叠计数、退化输入和长度不一致拒绝；全部通过。
- 下一轮实现相关峰的亚像素插值，并开始输出主峰、次峰与峰值突出度；仍保持离线候选路径。

## 2026-07-27 17:11

- 在独立 ZNCC 核心中增加三点抛物线亚像素峰值，输出 `bestShiftSubpixel` 和 `interpolationApplied`；边界、邻点缺失或非向下峰顶不进行插值。
- 增加非相邻局部次峰、次峰相关度和主次峰突出度诊断，周期剖面的等高峰能够被显式暴露，不再只返回单一最佳位移。
- 峰值核心仍不内置相关度、突出度或置信度接受阈值，避免用历史样本确定固定门限；接受策略留给后续结合动态窗口和扫描质量的诊断层。
- 亚像素测试采用 48 组随机连续位移和随机相位光滑剖面，评价整组平均误差改善与最坏误差，而不是围绕固定角度样本调优。
- 首次测试发现“每个场景都必须优于整数峰”断言过强；未修改算法或增加特例，改为总体性质指标后 ZNCC 7 个测试全部通过。
- 下一轮构建动态搜索窗口和置信度诊断层，加入周期性/多峰拒绝原因；仍不接入生产路径。

## 2026-07-27 17:25

- 新增独立 `trackProfileShift` 适配层，将扫描时间戳、设备最大允许角速度和每采样点角度转换为逐扫描对的动态 ZNCC 搜索窗口。
- 搜索上限按 `ceil(ωmax × Δt / degreesPerSample)` 实时计算；最低重叠点数按调用方显式重叠比例与当前剖面长度计算，没有固定像素窗口或历史角度范围。
- 时间戳倒序/相等、物理参数无效、重叠比例无效以及窗口达到整个剖面时返回结构化拒绝原因；不会静默截断过宽窗口。
- 成功结果同时输出时间差、最大允许角度变化、动态窗口、最低重叠点数、亚像素位移、角度变化和完整 ZNCC 诊断。
- 新增 3 个性质测试，覆盖 64 组随机物理工况的窗口推导、48 组窗口内随机平移恢复和各类非法输入拒绝，全部通过。
- 首次平移测试因亚像素结果相对整数真值偏移约 0.0013 点失败；未修改算法，改为分别验证整数主峰和亚像素容差。
- 下一轮实现置信度证据与周期/多峰歧义的结构化拒绝策略，阈值来源需保持可解释且不依赖历史角度标签。

## 2026-07-27 17:44

- 为特征追踪结果增加置信度证据向量，分别输出主峰相关度、有效重叠率、主次峰突出度、搜索边界状态和数值等价峰数量，不使用固定权重合成分数。
- 增加 `peakAtSearchBoundary` 拒绝：主峰触及动态物理窗口时不外推、不接受，完整保留 ZNCC 曲线和证据。
- 增加 `ambiguousEquivalentPeaks` 拒绝：使用由机器精度、相关值量级和候选数量推导的数值容差识别等价局部峰，不使用历史样本相关度门限。
- 周期剖面测试验证多个等高峰会被拒绝且突出度接近零；边界平移测试验证主峰位于窗口端点时被拒绝。
- 特征追踪测试扩展为 5 个并全部通过；既有随机动态窗口和平移性质测试继续通过。
- 近等价多峰仍未设置经验阈值，等待分布性质测试和独立现场证据；下一轮先扩展噪声、缺失和多峰场景的分布验证，量化证据指标而不调固定角度样本。

## 2026-07-27 18:05

- ZNCC 结果新增次峰有效重叠点数，置信度证据新增基于 Fisher 变换和重叠样本量标准误差的 `fisherPeakSeparation`。
- 峰间分离度是可跨扫描比较的统计证据，不作为固定接受阈值，也不合并进带经验权重的单一分数。
- 分布性质测试增加 64 组随机场景，联合比较低噪声、高噪声加缺失、唯一特征和独立扰动近周期剖面。
- 验证结果满足方向性性质：噪声增加使平均相关度降低，缺失增加使平均重叠率降低，近周期剖面的平均标准化峰间分离度低于唯一特征剖面。
- 首次近周期构造使用同一剖面与自身比较，主峰精确为 1 导致 Fisher 变换饱和；随后改为两次独立扰动的相邻扫描模型，未修改算法或针对固定角度调参。
- ZNCC 与特征追踪共 13 个测试全部通过；下一轮评估如何用生成分布建立数据驱动的近多峰拒绝边界，且必须与独立现场验收阈值分开。

## 2026-07-27 18:09

- 近多峰生产拒绝边界因缺少独立现场统计依据暂不固化，转入阶段 4 的独立角速度观测基础能力。
- 新增 `calculateAngularVelocityObservation`，使用特征追踪输出的真实 `elapsedMs` 和 `angleDeltaDeg` 计算有符号 `deg/s`，同时输出速度绝对值。
- 换向信号通过显式 `positive` / `negative` 期望方向参与一致性检查；冲突返回 `directionConflict`，保留原始速度符号，不强制翻转或取绝对值掩盖异常。
- 上游特征追踪拒绝会转化为 `trackingRejected`，并保留原始追踪拒绝原因和置信度证据；结构不完整时返回 `invalidTrackingResult`。
- 新增 3 个测试，覆盖 64 组随机时间间隔、角度分辨率和双向位移的速度计算、方向冲突保护及上游拒绝传播，全部通过。
- 下一轮实现多观测的稳健速度聚合，优先采用无固定调参中心的中位数与 MAD 诊断，并明确最少样本要求必须由调用方提供。

## 2026-07-27 18:23

- 新增 `aggregateAngularVelocityObservations`，只使用已接受、数值有效且期望方向一致的角速度观测。
- 聚合中心采用无权重中位数，同时输出有符号中位速度、速度绝对值中位数、MAD、相对 MAD 和最大绝对偏差；不使用固定权重或固定 MAD 倍数裁剪。
- `minimumAcceptedObservations` 必须由调用方显式提供，非法配置、有效观测不足和混合方向分别返回结构化拒绝原因。
- 新增 2 个聚合测试：64 组随机中心与离散度场景加入单个极端错误后，中位速度偏移不超过正常局部离散范围；同时验证最少样本和混合方向拒绝。
- 角速度测试扩展为 5 个并全部通过；下一轮处理换向附近观测的物理边界排除，裁剪时间必须来自实际行程/端部配置而非历史角度标签。

## 2026-07-27 20:33

- 新增 `selectSteadyStateAngularVelocityObservations`，使用完整行程起止时间和调用方显式提供的起止端排除时间构造稳态窗口。
- 只有参考扫描与候选扫描两个时间戳都完整落在稳态窗口内时才保留观测；跨越换向端部、越出行程和非法/倒序时间分别计数并输出诊断。
- 行程范围、端部时间和空稳态窗口均执行边界校验；本层不内置 12000/13000/20000ms 等历史动作，也不替调用方选择端部参数。
- 新增 3 个测试，覆盖确定性分类、64 组随机行程性质及非法配置拒绝；角速度测试扩展为 8 个并全部通过。
- AirRingServer typecheck 仍未通过，但错误均为既有 `fft-js` 声明缺失、旧测试 import assertion、历史文件失效导入和 `confirmCount` 类型不一致，本轮文件无新增错误。
- pnpm 依赖检查曾因 registry 与锁文件 tarball URL 不一致失败；改用锁文件对应的官方 npm registry 后恢复依赖，供应链校验通过，但项目策略仍拒绝自动执行原生依赖构建脚本。本轮测试不依赖这些原生构建。
- 下一轮继续阶段 4：为低置信度和速度波动拒绝标准寻找设备物理依据或独立统计依据，不按历史角度误差调阈值。

## 2026-07-27 20:45

- 新增 `evaluateAngularVelocityStability`，在已接受的角速度聚合结果上执行独立稳定性判定，不改变聚合中心或生产路径。
- 调用方必须显式提供相对 MAD 上限和最大绝对速度偏差上限；两项均执行有限值与非负边界校验，本层不提供经验默认值。
- 输出实际值、允许值、逐项 `violations` 和 `unstableVelocity` 拒绝原因；相对离散度和绝对偏差可单独或同时越界，不合成为固定权重分数。
- 新增 3 个测试，覆盖容差内接受、两类单独/联合越界及非法聚合和限值拒绝；角速度测试扩展为 11 个并全部通过。
- AirRingServer typecheck 仍仅报告既有 `fft-js` 声明、旧 import assertion、历史文件失效导入和 `confirmCount` 错误，本轮文件无新增类型诊断。
- 阶段 4 的速度不稳定拒绝机制已完成；生产容差数值仍需设备允许波动或独立现场统计确认。下一轮处理低置信度观测策略，继续保持证据与接受门限分离。

## 2026-07-27 20:59

- 新增 `evaluateFeatureTrackingConfidence`，使用调用方显式策略逐项评价相关度、重叠率及可选的峰值突出度和 Fisher 峰间分离度。
- 没有竞争次峰时，可选的突出度/Fisher 门限不参与拒绝；存在次峰但调用方要求 Fisher 且证据不可计算时，返回 `fisherPeakSeparationUnavailable`，不静默接受。
- `calculateAngularVelocityObservation` 新增可选置信度策略；未提供时保持原行为，提供后区分 `lowConfidence`、`invalidConfidenceEvidence` 和 `invalidConfidenceLimits`，并保留逐项 `confidenceViolations`。
- 新增 4 个测试，覆盖多证据联合越界、Fisher 缺失语义、非法输入及角速度观测传播；featureTracking 9 个与 angularVelocity 12 个测试共 21 个全部通过。
- 首次 typecheck 发现一处本轮测试夹具缺少 `confidenceViolations`，已补齐；复验后本轮文件无类型错误，项目仍仅剩既有 `fft-js`、旧 import assertion、历史失效导入和 `confirmCount` 错误。
- 阶段 4 的通用机制项现已全部完成，但生产端部时间、置信度门限和速度容差仍无独立依据，继续保持离线。下一轮应进入正反向速度一致性比较，完成阶段 4 最后一个验收能力。

## 2026-07-27 21:15

- 新增 `evaluateBidirectionalVelocityConsistency`，按聚合结果中的方向识别正向和反向速度绝对值，输入顺序不影响结果。
- 同时输出绝对速度差和以双方速度算术平均为基准的对称相对差；调用方必须显式提供两类设备容差，本层无默认值。
- 绝对差与相对差可单独或联合产生 `violations`，任一越界返回 `inconsistentVelocity`；无效聚合、同向输入和非法容差分别结构化拒绝。
- 新增 3 个测试，覆盖顺序不变性、容差内接受、两类单独/联合越界及非法输入；角速度测试扩展为 15 个并全部通过。
- AirRingServer typecheck 仍仅报告既有 `fft-js` 声明、旧 import assertion、历史失效导入和 `confirmCount` 错误，本轮文件无新增类型诊断。
- 阶段 4 的实现项和验收能力现已全部完成；生产端部时间、置信度门限、速度稳定性容差和双向一致性容差仍待独立现场依据。下一轮进入阶段 5，先实现不带历史常量的线性端部补偿数学核心。

## 2026-07-27 21:18

- 新增独立纯函数 `calculateLinearEndpointCompensatedAngle`，不复用 `timeToAngle.ts` 中固定 20 秒默认值和分段启发式，也不接入生产估算路径。
- 按线性加速、匀速、线性减速分别计算角度，并验证总角度等价于 `ωconst × (Thalf - (Tacc + Tdec) / 2)`。
- 调用方必须显式提供匀速角速度、单程时长、加减速时间及合法角度范围；速度/时间非法、无匀速区、角度范围非法和候选越界分别结构化拒绝。
- 候选角度越界时保留三段角度、匀速持续时间和总角度诊断，避免丢失问题来源。
- 新增 4 个测试，覆盖 64 组随机物理参数的公式性质、零端部退化、非法配置和角度越界诊断，全部通过。
- AirRingServer typecheck 仍仅报告既有 `fft-js` 声明、旧 import assertion、历史失效导入和 `confirmCount` 错误，新增模块与测试无类型诊断。
- 阶段 5 已完成线性补偿核心与边界保护；下一轮实现端部时间敏感性分析，量化参数变化对候选角度的影响，仍不选择生产端部时间。

## 2026-07-27 21:27

- 新增 `analyzeLinearEndpointSensitivity`，在线性模型上解析计算加速/减速时间每增加 1ms 对角度的变化 `-ω/2000` 度。
- 调用方显式提供两端时间扰动范围；输出两端独立角度影响、联合最坏影响、最小/最大角度和总跨度，不设置默认不确定度。
- 对称扰动必须保证端部时间不会减为负数、增加后仍存在匀速区；扰动角度越出合法范围时返回 `perturbedAngleOutOfRange` 并保留完整敏感性诊断。
- 新增 3 个测试，覆盖 64 组随机参数的解析性质、非法/退化扰动和扰动越界诊断；端部补偿测试扩展为 7 个并全部通过。
- AirRingServer typecheck 仍仅报告既有 `fft-js` 声明、旧 import assertion、历史失效导入和 `confirmCount` 错误，本轮文件无新增类型诊断。
- 阶段 5 只剩端部时间的可信来源与设备角度容差尚未确认；数学核心、边界保护和敏感性诊断已完成。下一轮应在不虚构配置值的前提下审计现有配置/数据是否存在可用端部时间来源。

## 2026-07-27 21:38

- 完成端部时间来源审计：上旋 S7 映射只有方向信号和 `MotorFrequency`，没有加速/减速时间配置字段；持久化链路只保存频率值。
- DS01～DS05 的历史 `motorFrequency` 全部为 0，且 PLC `INT` 值的实际单位、比例和最大频率未确认，当前不能据此测量斜坡时间。
- 仿真模块的 20 秒端部时间属于合成真值；ADBox 的加减速参数属于扫描器运动，均不得作为上旋生产参数。
- 新增 `resolveTrustedEndpointTiming`：只接受带来源标识的设备配置，或样本数、非零最大频率和单位确认均完整的实测频率证据；显式拒绝仿真和历史启发式。
- 端部补偿测试由 7 个扩展为 10 个并全部通过；生产接入仍等待真实 PLC 配置或有效频率采集。

## 2026-07-27 21:45

- 新增独立 `aggregateTripAngleCandidates`，仅聚合上游已接受且角度有限的完整行程候选，不接入生产路径。
- 聚合结果输出角度中位数、MAD、样本标准差、正反向各自中位数、方向间差值、有效/拒绝数量及每行程趋势。
- 调用方必须显式提供最少有效行程数和最大允许趋势；聚合层强制至少包含一个正向与一个反向行程。
- 首轮测试发现普通最小二乘趋势会被单个极端候选拉偏，随后改用 Theil–Sen 成对斜率中位数，未增加样本定向规则。
- 新增 5 个测试，覆盖离群稳健性、上游拒绝过滤、双向要求、趋势漂移及非法配置；全部通过。
- 下一轮应实现换向事件去抖和完整行程序列验证，为候选聚合提供可信的行程边界。

## 2026-07-27 21:49

- 新增独立 `segmentDebouncedCompleteTrips`，不修改 S7 连接、设备控制或现有生产 `buildTripSegment`。
- 正反转布尔信号必须互斥；双真/双假计为无效信号并打断连续稳定计数，不会触发换向。
- 换向必须连续满足调用方显式提供的稳定样本数，边界时间取该稳定序列的首个采样，而不是确认时刻。
- 采集开头和结尾视为开放的不完整片段；只有两个已确认换向边界之间的行程会输出。
- 行程时长上下限由调用方显式提供；过短和过长行程保留诊断但标记为拒绝，非递增时间戳和非法配置整体拒绝。
- 行程分段与候选聚合共 10 个测试全部通过。下一轮应把完整行程、速度聚合、可信端部时间和角度补偿组合为单行程候选构建器。

## 2026-07-27 21:53

- 新增独立 `buildSingleTripAngleCandidate`，组合已接受完整行程、稳健角速度聚合、可信端部时间和线性补偿，不接入生产入口。
- 候选输出最大角度、匀速速度绝对值、有效观测数、相对速度 MAD、行程时间范围及端部参数来源与 provenance。
- 行程拒绝、速度聚合拒绝、行程/速度方向冲突、不可信端部来源和端部补偿失败均分层传播为稳定拒绝原因，不使用默认值兜底。
- 即使后续端部补偿被拒绝，已验证的速度与离散度诊断仍会保留，便于定位失败层级。
- 单行程候选、端部补偿、行程分段和多行程聚合共 25 个测试全部通过。
- 当前仍不合成单一置信度分数；下一轮应设计基于观测证据分布的行程置信度向量或汇总统计，避免引入经验权重。

## 2026-07-27 22:00

- 新增 `aggregateTripConfidenceEvidence`，对行程内已接受角速度观测的特征追踪证据逐维聚合，不生成经验加权总分。
- 相关度、重叠率、峰突出度和 Fisher 峰间分离度分别输出有效数量、中位数与最小值。
- 单独统计缺失/非法证据、无竞争峰、竞争峰 Fisher 不可用、边界峰和等价峰观测数量，避免把不同语义折叠为同一个低分。
- 最少有效证据数量由调用方显式提供；本层不设置相关度或突出度生产阈值。
- 单行程候选现在要求已接受的置信度聚合，并携带完整结构化证据；删除了无定义的标量置信度语义。
- 行程置信度、单行程候选和多行程聚合共 16 个直接相关测试全部通过。
- Stage 6 的实现项已完成；设备验收容差和真实生产端部时间仍需独立现场依据。下一轮可进入 Stage 7 的通用目标函数局部精调基础能力。

## 2026-07-27 22:08

- 进入 Stage 7，新增独立 `buildDynamicLocalSearchWindow`，暂不修改现有生产搜索路径。
- 局部半径取调用方显式不确定度分量之和与显式最小半径的较大值，不使用 DS 角度带、固定局部半径或经验权重。
- 窗口裁剪到全局合法角度范围，并输出左右实际半径、跨度及两侧裁剪状态；特征候选本身越界时直接拒绝。
- 依据显式搜索步长计算计划搜索点数；窗口退化或超过调用方搜索预算时拒绝，不自动放大步长掩盖计算成本。
- 新增 5 个测试，包含 64 组随机候选性质、单侧边界裁剪、预算超限、退化窗口和非法参数；全部通过。
- 下一轮应实现对任意通用目标函数的局部离散扫描与边界最优保护，仍不接入生产路径。

## 2026-07-28 22:00

- 新增独立 `scanLocalObjective`，直接消费经预算验证的动态窗口和任意 `angle → loss` 回调，不耦合 tuned 规则或历史角度带。
- 离散网格覆盖窗口两端，实际评估数受窗口计划预算约束；动态窗口结果新增显式 `searchStepDeg` 供扫描复用。
- 非有限 loss 和目标函数异常计入无效评估；部分无效时继续使用有限证据，全部无效时返回 `noFiniteObjective`。
- 最佳有限 loss 位于任一窗口边界时返回 `bestAtBoundary`，保留最佳角度、loss 和完整扫描曲线，不把疑似窗口外极值当作成功结果。
- 动态窗口与局部扫描共 10 个测试全部通过，覆盖凸目标恢复、双侧边界、部分/全部无效目标和无效窗口。
- 下一轮应在离散内部最优点附近实现有界黄金分割细化，并验证细化结果仍不贴局部或全局边界。

## 2026-07-28 22:04

- 新增 `refineLocalObjectiveWithGoldenSection`，只在已接受离散内部最优点的最近有限左右支撑点之间细化，不跨越动态窗口。
- 角度收敛容差、最大迭代数和最小边界距离均由调用方显式提供；不存在固定 0.01° 或 ±1° 生产默认动作。
- 任一侧缺少有限支撑、细化目标异常/非有限、迭代预算不足和边界保护距离不足均结构化拒绝。
- 输出最终括区间、跨度、迭代数、细化角度/loss、相对离散最佳 loss 的变化和窗口边界距离。
- Stage 7 动态窗口、离散扫描和黄金分割共 16 个测试全部通过，其中连续极值恢复覆盖 32 组随机凸目标。
- 下一轮应构建离线通用精调管线，将窗口、扫描和黄金分割组合为单一诊断结果；之后再评估如何安全适配 `evaluateExpanded`。

## 2026-07-28 22:19

- 新增 `refineFeatureAngleWithLocalObjective`，将动态窗口、局部离散扫描、黄金分割和证据一致性检查组合为单一离线纯函数入口。
- 每层失败后停止后续计算，并输出 `rejectStage`、稳定拒绝原因及此前所有阶段的完整诊断。
- 输出特征角度、细化角度、两者绝对偏移、最终 loss、总不确定度和 `featureCandidate`/`genericObjective` 证据来源。
- 特征与目标函数最大允许偏移由调用方显式提供；偏移超限返回 `featureObjectiveConflict`，不选择任一结果兜底。
- Stage 7 四个模块共 22 个测试全部通过，覆盖成功链路、窗口/扫描/细化失败及双证据冲突。
- 下一轮应实现 `evaluateExpanded` 的只读目标适配器，将现有 expanded 数据转成该管线的 `angle → loss` 回调；继续保持不接入默认生产路径。

## 2026-07-28 22:24

- 新增 `createExpandedObjective`，把现有 `evaluateExpanded` 只读适配为 Stage 7 通用局部精调所需的 `angle → loss` 回调，未修改默认生产搜索路径。
- 适配层复制行程和点，不持有调用方可变数组；`y=NaN` 继续表示合法出界缺测，非有限角度输入直接返回 `Infinity`。
- 行程时长、梯形加速比例、点时间范围、offset 和厚度数值均执行结构化校验；非法点不被静默过滤后继续估算。
- bin 数、最少有效行程数和最少有限厚度点数均由调用方显式提供，不增加历史样本默认值。
- 新增 5 个适配器测试；与局部精调测试合计 11/11 通过，覆盖目标等价、输入只读、缺测计数、无效输入、证据不足及管线兼容。
- 下一轮应把 Stage 6 的多行程候选和置信度证据整理为 Stage 7 粗估输入适配层，仍不选择生产容差或切换默认路径。

## 2026-07-28 22:29

- 新增 `adaptTripCandidatesToFeatureRefinement`，将 Stage 6 已接受的多行程中位角度只读适配为 Stage 7 的 `θfeature` 与动态窗口输入。
- 局部窗口不确定度保留为四个具名分量：行程角度 MAD、正反向中位数差、`|Theil–Sen 趋势| × 已观测间隔数` 和调用方显式提供的端部时间角度不确定度。
- 适配层不合成置信权重分数；逐行程置信证据只用于完整性门禁，并输出总证据数、最小相关度和最小重叠率诊断。
- 聚合拒绝、诊断非法、候选数量不一致、候选角度非法、置信证据缺失和端部不确定度非法均结构化拒绝。
- 新增 5 个测试，与多行程聚合和动态窗口测试合计 15/15 通过；另复验适配器与动态窗口直接连接 5/5 通过。
- 下一轮应构建 Stage 6 粗估适配器、expanded 目标适配器与局部精调器的单一离线编排入口，并保持生产默认路径不变。

## 2026-07-28 22:50

- 新增 `refineUpperRotationAngleOffline`，按“Stage 6 特征粗估适配 → expanded 目标适配 → 局部扫描/黄金分割精调”顺序提供单一离线入口。
- 任一层拒绝都会停止后续计算，并输出 `featureCandidate`、`expandedObjective`、`localRefinement` 完整诊断及稳定的顶层拒绝阶段/原因。
- 成功结果统一输出最终角度和 loss；所有窗口、证据容差、bin 数、点数要求和迭代预算仍由调用方显式提供。
- 首轮单轨迹合成信号在窗口上边界获得最优 loss，被既有边界保护正确拒绝；测试随后改为多 offset 的扫描展开物理模型，没有放宽算法保护。
- 离线编排和三个直接依赖组件共 20/20 测试通过，覆盖成功只读链路及三层短路拒绝。
- Stage 7 离线能力已闭环，且未导入 DS 编号、固定目标角度带或融合系数；下一轮进入 Stage 8，先定义不改变生产输出的影子运行记录与批量统计核心。

## 2026-07-28 23:16

- 进入 Stage 8，新增 `buildUpperRotationShadowRecord`，从现有 tuned/generic 对照和新离线候选中提取可序列化标量快照，不保存目标回调、扫描曲线或 expanded 大数组。
- 每条记录固定 `selectedSource='production'`，选中角度只取 tuned 生产结果；generic 与新候选均不覆盖生产输出。
- 记录三路径接受状态、角度、耗时、拒绝阶段/原因及三组有符号路径差；任一路径拒绝时对应差值保持 `null`。
- 新增 `aggregateUpperRotationShadowRecords`，输出三路径接受率、两两绝对差的中位数/P95/最大值、耗时分布和新路径拒绝原因计数。
- 批量统计明确属于无标签一致性证据，不把与生产/generic 接近解释为准确度；最低记录数由调用方显式提供。
- 影子统计与离线编排共 9/9 测试通过；下一轮应增强影子记录批次的工况覆盖统计与重复 window ID/时间顺序完整性检查，之后再评估持久化接入点。

## 2026-07-28 23:45

- 影子批量统计改用显式 `UpperRotationShadowBatchOptions`，最低记录数、最低机台数、最低配方数及是否要求完整覆盖元数据均由调用方提供。
- 批次统计前检查 window ID 唯一、观测时间严格递增、三路径快照数值一致性及生产选择未被改写；异常批次整体结构化拒绝。
- 新增首末观测时间、时间跨度、不同机台/配方数量、缺失元数据数量和按机台/配方记录数统计。
- 元数据缺失可按策略只记录诊断或直接拒绝；机台/配方覆盖不足分别返回稳定拒绝原因，不用当前少量数据推导默认要求。
- 影子批次与离线编排回归共 11/11 测试通过，覆盖重复 ID、同刻/乱序时间、非法记录、元数据缺失和覆盖不足。
- 下一轮应扩展影子元数据的速度、角度区间、膜宽与工况来源字段，并实现连续数值覆盖分桶；仍不接入持久化调用点。

## 2026-07-29 00:20

- 影子记录元数据新增旋转速度、膜宽和工况来源；字段继续保持可选、可序列化，并校验速度/膜宽必须为有限正数。
- 批量配置新增旋转速度、生产选中角度和膜宽三组显式分桶边界；边界必须有限、严格递增且至少包含两个端点。
- 分桶采用左闭右开、最后一桶右闭语义，分别输出桶计数、缺失计数和范围外计数，不把未知或超界值并入边缘桶。
- 新增按 `deviceConfiguration`/`measured`/`simulation` 工况来源计数；可显式要求所有连续工况和来源完整，否则结构化拒绝。
- 连续工况覆盖测试扩展为 9/9 通过，覆盖精确边界归属、缺失/超界诊断、完整性门禁和非法分桶。
- 下一轮应为每个连续工况桶增加三路径接受率与差值摘要，使影子分析能定位特定速度/角度/膜宽区域的退化，而非只看全局统计。

## 2026-07-29 21:21

- 新增独立 `upperRotation.shadowConditionStatistics.ts`，避免把每桶分析继续堆入影子批量统计主模块。
- 旋转速度、生产选中角度和膜宽的每个显式桶现在分别输出记录数、三路径接受数/接受率、三组绝对角度差的中位数/P95/最大值及新候选拒绝原因计数。
- 空桶仍保留边界和 `recordCount=0`，接受率为 `null`；不会用 0% 混淆“有样本全部拒绝”和“完全没有覆盖”。
- 缺失或范围外工况继续只进入覆盖诊断，不会被塞入任一局部统计桶；最后一桶明确包含右端点。
- 影子统计 9/9 测试通过，并增加速度桶局部拒绝、局部差值、角度桶接受率和空膜宽桶语义断言。
- 下一轮应增加“每桶最少记录数”充足性标记和覆盖盲区清单，防止对极少样本桶过度解读。

## 2026-07-29 21:24

- 影子批量配置新增显式 `minimumRecordsPerConditionBin`，只接受正整数，不从当前样本量推导默认值。
- 每个速度、生产角度和膜宽桶新增最低记录数、样本缺口及 `empty`/`insufficient`/`sufficient` 状态。
- 批量结果新增结构化 `conditionCoverageGaps`，列出所有空桶和少样本桶的维度、边界、现有数量与缺口。
- 覆盖盲区不会自动令无标签批量统计失败，也不会改变生产选择；它只阻止将对应桶的局部表现解读为充分证据。
- 影子统计 9/9 测试通过，覆盖少样本、充分、空桶、盲区清单和非法最低桶样本数。
- 下一轮应增加工况交叉覆盖（至少速度×角度）的稀疏矩阵，避免各单维均覆盖但关键组合从未采集。

## 2026-07-29 21:31

- 新增速度 × 生产选中角度二维覆盖矩阵，按行优先保留全部显式组合，最后一行和最后一列包含各自右端点。
- 每个交叉单元输出独立最低记录数、样本缺口、覆盖状态、三路径接受率、三组差值摘要和候选拒绝原因；空单元接受率保持 `null`。
- 二维单元最低样本数通过 `minimumRecordsPerConditionCell` 显式配置，不复用一维桶阈值，也不从当前样本推导。
- 当前 3 × 3 测试矩阵正确识别 1 个充分单元和 8 个覆盖缺口，所有单元记录数总和与批次有效记录数一致。
- Stage 7+8 联合回归 13/13 通过；AirRingServer typecheck 未新增本轮诊断，仍仅有 `fft-js` 声明、旧 import assertion、历史模块导入和 `confirmCount` 等既有问题。
- 下一轮应将一维与二维覆盖缺口转换为确定性的补采优先级清单，优先空组合，再按样本缺口排序，避免验收人员手工解释矩阵。

## 2026-07-29 21:38

- 新增独立纯函数模块 `upperRotation.shadowCoverageTargets.ts`，将一维桶和速度 × 生产角度单元缺口转换为统一、可序列化的补采目标。
- 排序固定为：空覆盖优先、记录缺口降序、同等条件下二维组合优先，最后按固定维度和桶索引排序；输入数组反转不会改变结果。
- 每个目标输出连续 `priority`、边界、现有记录数和 `additionalRecordsNeeded`，但不输出会重复计算重叠目标的总需采集记录数。
- 影子批量结果新增 `coverageCollectionTargets`；当前测试批次从 8 个二维和 6 个一维缺口生成 14 个连续编号目标，不影响批次接受或生产选择。
- 新增补采规划 2 个测试；Stage 7+8 联合回归扩大为 15/15 通过，类型感知 lint 通过，typecheck 未新增本轮诊断。
- 下一轮应增加与批次接受解耦的覆盖准备度摘要，直接报告空目标数、不足目标数和是否满足覆盖计划，避免调用方自行扫描完整清单。

## 2026-07-29 21:44

- 补采目标模块新增 `ShadowCoverageReadinessSummary`，明确区分 `notEvaluated`、`incomplete` 和 `satisfied` 三种覆盖状态。
- 已评估摘要直接输出总目标、空目标、不足目标、一维目标和二维目标数量，并把清单首项作为 `highestPriorityTarget`。
- 影子批量结果新增 `coverageReadiness`；配置、记录数或记录完整性在覆盖计算前失败时所有计数保持 `null`，不会因空目标数组误报满足。
- `coverageRequirementsSatisfied` 仅代表显式一维桶与速度 × 生产角度单元样本阈值，不影响批次接受、算法准确度结论或生产选择。
- 覆盖摘要与影子统计定向测试 11/11、Stage 7+8 联合回归 15/15、类型感知 lint 均通过；typecheck 未新增本轮诊断。
- 下一轮应增加按机台和配方分组的覆盖准备度，避免全局样本量满足时掩盖单一机台或配方的局部覆盖缺口。
